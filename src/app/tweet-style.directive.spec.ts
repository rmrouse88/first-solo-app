import { TweetStyleDirective } from './tweet-style.directive';

describe('TweetStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new TweetStyleDirective();
    expect(directive).toBeTruthy();
  });
});
